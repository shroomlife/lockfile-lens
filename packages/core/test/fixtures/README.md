# Fixtures

`real-shaped/` contains reduced Bun 1.2+ JSONC lockfile fragments that preserve the
documented `workspaces` and compact `packages` tuple structure. They are intentionally
small so the initial parser, diff, classifier, and CLI behavior can be verified before
project-specific rltytrck fixtures are added.
